{
  "worktree": {
    "bgIsolation": "none"
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // empty' 2>/dev/null); echo \"$FILE\" | grep -q 'packages/parser/src' && npm run build:parser 2>&1 | tail -5; true",
            "timeout": 60,
            "async": true
          }
        ]
      }
    ]
  }
}
