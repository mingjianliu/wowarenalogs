# F193 V2 — pending LLM A/B evaluation plan

Status: deterministic verification complete (unit tests, corpus diff, regression gate); the
rubric-behavior change (trade findings now allowed with a [CONTESTED] anchor) is NOT covered by
deterministic checks and needs a judged A/B before the flag ships to prod unmonitored.

## What to run (mirrors the healer-offense V1 adoption run, eval-ledger 2026-07-08 row)

1. Control = branch point before the F193 commit; treatment = after. Only `V2_CONTESTED_TRADES`
   - the rubric bullets differ (verify with `git diff <control>..<treatment> -- packages/shared/src/prompts packages/shared/src/utils/healerOffenseAnalysis.ts`).
2. Corpus: owner healer prompts (the 1081-prompt user corpus). Stratify ~20 pairs, REQUIRING
   ≥10 prompts whose `<healer_offense>` block contains a `[CONTESTED]` line (the change is
   invisible elsewhere — an unstratified sample wastes most pairs).
3. Responders + blind judge per `/improve-healer-prompts` (AB_DIR-isolated harness). Judge
   dimensions to watch: **labelBias / accuracy** (the failure mode is FALSE CRITICISM of a
   legitimate defensive choice — a finding that faults healing during 70–85% pressure without
   citing the [CONTESTED] anchor is an automatic defect), plus focusCalibration.
4. Deterministic post-check on treatment responses: every trade-shaped finding must quote a
   `[CONTESTED]` time range verbatim (grep-able); any ally-below-70% trade finding = hard fail.
5. Eval-integrity rules apply (no scripted scores; partial honest data beats fabricated
   completeness). Record the outcome in docs/eval-ledger.md.

## Why not run tonight

Session-token cost (responders + 2×20 blind judge passes ride the Claude session) — user asked to
be consulted before spending it. The V2 flag is ON in the branch; if the A/B must precede exposure,
flip `HEALER_OFFENSE_FLAGS.V2_CONTESTED_TRADES` to false pre-merge instead of reverting code.
